# tiny-simple-app-rollback

Run the app from this directory using a local static server.

## Option 1: Node.js
npx serve .

Then open the URL shown in the terminal (usually http://localhost:3000).

## Option 2: Python 3
python3 -m http.server 8000

Then open http://localhost:8000.
