{
  "error_taxonomy": {
    "ContractMismatch": "Implementation deviates from node intent.",
    "ValidationFailure": "Validation command failed."
  },
  "examples": [
    {
      "node_id": "styling_impl",
      "steps": [
        "Create styles.css with a centered layout and styled buttons."
      ],
      "target_files": [
        "styles.css"
      ]
    }
  ],
  "inputs": [
    {
      "name": "depends_on",
      "type": "ui_contract"
    }
  ],
  "invariants": [
    "Modify only files listed in node new_files/modified_files.",
    "Preserve dependency expectations from upstream nodes.",
    "Implementation must follow the declared node steps."
  ],
  "node_id": "styling_impl",
  "outputs": [
    {
      "name": "new_file",
      "type": "styles.css"
    }
  ],
  "public_api": [
    "No explicit validation command declared."
  ],
  "purpose": "Apply CSS to layout the calculator and provide visual feedback for interactions."
}
