{
  "error_taxonomy": {
    "ContractMismatch": "Implementation deviates from node intent.",
    "ValidationFailure": "Validation command failed."
  },
  "examples": [
    {
      "node_id": "contract_api_logic",
      "steps": [
        "Define the internal state object structure (currentValue, previousValue, operator).",
        "Specify the signatures for calculate(a, b, op) function."
      ],
      "target_files": []
    }
  ],
  "inputs": [],
  "invariants": [
    "Modify only files listed in node new_files/modified_files.",
    "Preserve dependency expectations from upstream nodes.",
    "Implementation must follow the declared node steps."
  ],
  "node_id": "contract_api_logic",
  "outputs": [],
  "public_api": [
    "No explicit validation command declared."
  ],
  "purpose": "Define the interface for the calculation functions and state management in JavaScript."
}
